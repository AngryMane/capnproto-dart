{
  "name": "capnproto-dart",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "--cap-add=SYS_ADMIN"
  ],

  // Start the host-side notification relay (see notify-relay/) before the
  // container exists. initializeCommand runs on the HOST, every time the
  // container is (re)built or started, and the start script is idempotent.
  "initializeCommand": [
    "bash",
    "${localWorkspaceFolder}/.devcontainer/notify-relay/start_host_relay.sh"
  ],

  // Mount Cargo registry/git cache outside the container so it survives rebuilds.
  // Also bind-mount the host's Claude Code / Codex CLI credential directories so
  // logging in once on the host carries over into the container (no re-login).
  // This is a live two-way bind, not a one-time copy: session/history files the
  // CLIs write inside the container land in these same host directories too.
  // Only works if the host stores the session as a file here rather than in an
  // OS keychain (macOS Keychain / Windows Credential Manager) — both tools fall
  // back to file storage by default when no OS keyring is available (e.g. this
  // Linux container), but a macOS host may need one `claude login` / `codex login`
  // inside the container the first time if it was using Keychain-backed storage.

  // Forward the host's display sockets so GUI apps (e.g. `flutter run -d linux`)
  // can show a window on the host's screen. Assumes a native Linux host with
  // XDG_RUNTIME_DIR set (true for any normal login session). The container
  // runs as root, so it can open the host user's Wayland/X11 socket files
  // even though it doesn't share the host user's UID — this stops working if
  // a non-root remoteUser is ever added here without also matching UIDs.
  "containerEnv": {
    "DISPLAY": "${localEnv:DISPLAY}",
    "WAYLAND_DISPLAY": "${localEnv:WAYLAND_DISPLAY}",
    "XDG_RUNTIME_DIR": "${localEnv:XDG_RUNTIME_DIR}"
  },

  "mounts": [
    "source=capnproto-dart-cargo-registry,target=/usr/local/cargo/registry,type=volume",
    "source=capnproto-dart-cargo-git,target=/usr/local/cargo/git,type=volume",
    "source=${localEnv:HOME}/.claude,target=/root/.claude,type=bind",
    "source=${localEnv:HOME}/.codex,target=/root/.codex,type=bind",
    // Bind-mount the host's SSH keys/known_hosts so `git pull`/`git push` over
    // SSH work inside the container without a separate key setup.
    "source=${localEnv:HOME}/.ssh,target=/root/.ssh,type=bind",
    // Share the host's GitHub CLI configuration so `gh auth login`/`gh auth status`
    // work inside the container without re-authenticating.
    "source=${localEnv:HOME}/.config/gh,target=/root/.config/gh,type=bind",
    // Wayland compositor socket (native rendering) — only this one socket
    // file, not the whole XDG_RUNTIME_DIR (which also holds the host's
    // D-Bus session bus, PipeWire, gnome-keyring, etc.) — and the X11 socket
    // dir (XWayland fallback for any tool that only speaks X11).
    "source=${localEnv:XDG_RUNTIME_DIR}/${localEnv:WAYLAND_DISPLAY},target=${localEnv:XDG_RUNTIME_DIR}/${localEnv:WAYLAND_DISPLAY},type=bind",
    "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind",

    // Desktop notifications: dedicated socket to the host relay started by
    // initializeCommand (see notify-relay/), not the D-Bus session bus. The
    // container can only send {title, body} over this socket via the
    // notify-host CLI; it can't reach any other host D-Bus service.
    "source=${localEnv:XDG_RUNTIME_DIR}/capnproto-dart-notify,target=/run/host-notify,type=bind,readonly"
  ],

  // 3000: Docusaurus dev server (website/, `npm run start`).
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Docusaurus docs site",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "anthropic.claude-code",
        "openai.chatgpt"
      ],
      "settings": {
        "dart.sdkPath": "/opt/dart-sdk",
        "[rust]": {
          "editor.defaultFormatter": "rust-lang.rust-analyzer"
        }
      }
    }
  },

  // Fetch Dart package dependencies after the container is created.
  "postCreateCommand": "find . -name pubspec.yaml -not -path '*/.*' -execdir dart pub get \\;"
}
